# Create the README, section by section
. "${SHELL_DIR}"/intro.sh
. "${SHELL_DIR}"/table_of_contents.sh
. "${SHELL_DIR}"/states.sh
. "${SHELL_DIR}"/transition_types.sh
. "${SHELL_DIR}"/transition_rules.sh
